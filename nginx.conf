server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # Redirect root to the app home page
    location = / {
        return 301 /frontend/index.html;
    }

    # Serve all frontend static files
    location /frontend/ {
        try_files $uri $uri/ =404;
    }
}
