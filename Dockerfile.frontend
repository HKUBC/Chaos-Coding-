FROM nginx:alpine

# Copy all frontend static files
COPY frontend/ /usr/share/nginx/html/frontend/

# Custom Nginx config: redirect / to the app, serve everything under /frontend/
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
